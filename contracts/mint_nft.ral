import "nft_collection.ral"

TxScript Main(
    nftCollectionContractId: ByteVec,
    name: ByteVec,
    description: ByteVec,
    uri: ByteVec
) {
    let nftCollection = NFTCollection(nftCollectionContractId)
    approveAlph!(callerAddress!(), 1200000000000000000)
    nftCollection.mint(name, description, uri)
}
