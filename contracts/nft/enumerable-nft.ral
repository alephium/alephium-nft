// Only 1 token is supported
Contract EnumerableNFT(
    collection: NFTPreDesignedCollection,
    nftIndex: U256
) implements INFT {
    // name; description; image
    pub fn getTokenUri() -> ByteVec {
        checkCaller!(true, 0)
        return collection.getTokenUri(nftIndex)
    }
}
