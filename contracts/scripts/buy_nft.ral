TxScript BuyNFT(
    totalPayment: U256,
    tokenId: ByteVec,
    nftMarketplaceContractId: ByteVec
) {
    assert!(totalPayment > 0, 0)

    let nftMarketplace = NFTMarketPlace(nftMarketplaceContractId)

    nftMarketplace.buyNFT{callerAddress!() -> ALPH: totalPayment}(tokenId, totalPayment)
}
