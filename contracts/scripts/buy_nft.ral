TxScript BuyNFT(
    totalPayment: U256,
    tokenId: ByteVec,
    collectionId: ByteVec,
    nftMarketplace: NFTMarketPlace
) {
    nftMarketplace.buyNFT{callerAddress!() -> ALPH: totalPayment}(tokenId, collectionId)
}
