TxScript BuyNFT(
    totalPayment: U256,
    tokenId: ByteVec,
    nftMarketplaceContractId: ByteVec
) {
    let nftMarketplace = NFTMarketPlace(nftMarketplaceContractId)

    nftMarketplace.buyNFT{callerAddress!() -> ALPH: totalPayment}(tokenId)
}
