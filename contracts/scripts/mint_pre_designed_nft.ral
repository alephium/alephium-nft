TxScript MintPreDesignedNFT(
    nftCollectionContractId: ByteVec
) {
    let nftCollection = NFTPreDesignedCollection(nftCollectionContractId)
    nftCollection.mint{callerAddress!() -> ALPH: 1 alph}()
}
